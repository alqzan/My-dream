// ===================== ألوان «مدار» — المصدر الواحد =====================
// نظيرُ `nav.ts` للتنقّل: قيمةٌ واحدةٌ في مكانٍ واحد.
//
// **لماذا وُجد هذا الملف؟** ألوانُ الأقسام والأسطح كانت مكتوبةً بأرقامها
// السداسية يدوياً في عشرات الملفّات (نحو 260 موضعاً للقيم المشتركة وحدها)،
// وهي نفسها المعرَّفة في `tailwind.config.ts` — فتغييرُ لون قسمٍ واحد كان
// يتطلّب تعقُّبَه في كلّ ملفّ، وأيُّ موضعٍ يُنسى يبقى على اللون القديم بلا أن
// يكسر شيئاً ظاهراً.
//
// **متى تُستعمل هذه القيم ومتى تُستعمل أصناف Tailwind؟**
//   • في JSX العادي استعمل الأصناف (`text-quran` · `bg-finance`) — لا تستورد
//     من هنا لتكتب `style={{ color: ... }}` بلا داعٍ.
//   • هذه القيم **لِما لا تصله الأصناف**: سمات SVG (`fill`/`stroke`)، وقيمُ
//     `style` السطرية، والألوان المارّة إلى `canvas` أو إلى مكوّناتٍ تأخذ
//     اللون خاصيّةً (`color="#..."`).
//
// **ما لا يدخل هنا**: لوحةٌ خاصّةٌ بمشهدٍ واحد لا تُشارَك — كسماء الذكريات
// (بنفسجُ الليل ونجومُه) أو تدرّجاتُ بطاقةٍ بعينها. تلك تبقى ثوابتَ مسمّاةً
// عند مكانها؛ رفعُها إلى «لوحةٍ عامّة» يجعلها تبدو مشتركةً وهي ليست كذلك.
//
// القيم **مطابقةٌ حرفياً** لما في `tailwind.config.ts` و`globals.css`؛ أيّ
// تغييرٍ لأحدهما يتبعه الآخر.

/** ألوان الأقسام — نفسها في `tailwind.config.ts` وفي `nav.ts`. */
export const SECTION = {
  /** الذهبيّ الأندلسيّ — لون العلامة (brand-500). */
  brand: "#c9852a",
  journal: "#8a6fb0",
  finance: "#3d9640",
  reading: "#c1663f",
  prayer: "#1f7a6c",
  /** أخضرٌ إسلاميّ عميق — مميَّزٌ عن أخضر المال العشبيّ وتركواز الصلاة. */
  quran: "#1b6b4c",
} as const;

/** أسطحُ الوضع الليلي وحدوده — نفسها في كتلة `.dark` في `globals.css`. */
export const DARK = {
  /** خلفيةُ الصفحة. */
  bg: "#171009",
  /** سطحُ البطاقة (نظير `bg-white` في الفاتح). */
  card: "#241c12",
  /** سطحٌ أخفت درجةً (نظير `bg-gray-50`). */
  cardMuted: "#2c2318",
  /** سطحٌ أخفت درجتين (نظير `bg-gray-100`). */
  cardSunken: "#382c1d",
  /** حدٌّ خفيف (نظير `border-gray-100`). */
  border: "#3a2e1e",
} as const;

/** أسطحُ الوضع الفاتح. */
export const LIGHT = {
  /** الكريميّ الدافئ — خلفيةُ الصفحة. */
  bg: "#f4eee2",
} as const;

/** ذهبٌ أفتح للأقواس والنجوم والتدرّجات — يقرأ على الداكن حيث يعتم brand. */
export const GOLD_LIGHT = "#e8b15a";

/** درجاتٌ أعمق **لخلفيةٍ مصمتة يقع عليها نصٌّ أبيض** (حبّة اليوم المُعلَّم في
 *  تقويم السلسلة مثلاً). ألوانُ `SECTION` متوسّطة النصوع، فالأبيض عليها يقرأ
 *  بين 3.06 و4.22 — تحت حدّ AA. هذه أعتمُ درجةٍ تُمرّر الأبيض مع بقاء الصبغة
 *  (أعتم بـ4٪ للمذكرات و7٪ للقراءة — فرقٌ لا تكاد العين تلحظه).
 *  **لا تستعملها للنصّ نفسه** — النصّ يأخذ `SECTION` أو صنف Tailwind. */
export const SECTION_DEEP = {
  brand: "#a16a22",
  reading: "#b35f3b",
  journal: "#846ba9",
} as const;
