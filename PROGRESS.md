# PROGRESS

سجل موجز لأهم ما أُنجز في المشروع. الحالة العامة: البناء أخضر (`npm run build`)
والتطبيق يعمل بالكامل على الجهاز مع مزامنة سحابية اختيارية.

## المزامنة والنسخ الاحتياطي

- [x] دمج متعدد الأجهزة بدل «آخر كاتب يفوز» (`mergeAppData` في `sync.ts`): توحيد كل
      المجموعات بالمعرّف/المفتاح، والأحدث يفوز في تعارض العنصر؛ سجلات العادات
      وإيداعات الاحتياطي وصلوات اليوم تُوحَّد بلا فقد.
- [x] إعادة محاولة المزامنة بتراجع أسّي (2s→30s) مع تنبيه واحد عند الفشل.
- [x] حارس حجم مستند Firestore قبل حد 1MB (تنبيه مبكر مرة واحدة).
- [x] وسائط سحابية في **Cloudflare R2** عبر وسيط Worker (صور/صوت بمفتاح تجزئة +
      manifest)، والمستند الرئيسي يحمل مراجع خفيفة فقط. (Firebase Cloud Storage
      كان المخزن الأول وقد استُبدل؛ الملفات القديمة تُقرأ منه للتوافق فقط.)
- [x] نسخة احتياطية JSON: تصدير بكل الصور مضمّنة + استيراد مُتحقَّق منه مع اختيار
      «دمج» أو «استبدال» وتراجع.
- [x] **تصدير مشفّر اختياري** بكلمة مرور (AES-GCM + PBKDF2) — يُكتشف تلقائياً عند
      الاستيراد ويطلب كلمة المرور (`lib/backupCrypto.ts`).
- [x] إخراج معرّف مساحة المزامنة السري من الكود (`getSyncSpace()`؛ يُحفظ لكل جهاز
      عبر بطاقة «مفتاح المزامنة»).
- [x] **قرار تبنّي لقطة السحابة يعتبر `revision`** (`syncDecision.ts`، دوالٌّ نقيّة
      مختبَرة): تعديلُ عنصرٍ قائم من جهازٍ ساعته متأخّرة لم يكن يُدمَج — لا معرّف
      جديد ولا شاهد جديد والختم يبدو أقدم؛ عدّاد المعاملة يكشفه.

## الأموال

> **أبوابٌ حُذفت بقرار المالك (٠٫١٫٣٨٧):** الأقساط · الالتزامات المتكرّرة ·
> الأصول والإهلاك · الرفّ · بوصلة التوصيات. حُذفت لأنّها كانت تصف التزاماً أو
> تعرض حساباً ولا تصرف ريالاً. نصُّها كلُّه محفوظٌ في تاريخ git عند `0d860ea`
> **مرجعاً يُقرأ لا قالباً يُنسَخ** — راجع `CLAUDE.md`. وهذا الملفّ كان ما زال
> يصفها «مُنجَزةً» بعد الحذف بثلاثة عشر إصداراً، وهو أسوأُ من نقصٍ في التوثيق:
> أوّلُ ما يقرؤه وكيلٌ جديد يناقض القاعدة المُلزِمة.

- [x] **البدل اليومي** (`computeDailyBudgetStatus`): تراكميٌّ من بداية الدورة،
      مع `carryAdjust` بعد ترحيل الفائض أو نزول الراتب.
- [x] **السقوف** (`budgetStatus.ts` — المصدر الوحيد): «كم صُرف من سقف هذا القسم
      داخل نافذة الحساب؟» يُجاب مرّةً واحدة وكلُّ واجهةٍ تقرأ منه، فلا تتناقض
      شاشتان. والنافذة يختارها المالك: دورةُ الراتب أو الشهرُ الميلادي.
- [x] **الاحتياطيات**: صناديقُ بإيداعاتٍ تُدمج بمعرّفاتها وتُشهَد عند الحذف،
      و«الفوائض» يستقبل باقي البدل اليومي عند نزول الراتب.
- [x] **الصرف النقديّ بوّابةٌ واحدة** (`cashOut`): أيُّ تجميعٍ للصرف يمرّ بها ولا
      يجمع `t.amount` مباشرةً — وهي المكان الذي يتغيّر لو عاد مفهوم «التزامٌ لم
      يُدفع»، بدل مطاردة عشرات المجاميع.
- [x] **قواعدُ التجار المتعلَّمة**: تصنيفُ مصروفٍ بيدك يُحفظ للتاجر، فالمرّة
      القادمة من المكان نفسِه تُصنَّف على طريقتك.

## الإصدار

- [x] رقم الإصدار = عدد التعديلات الجوهرية منذ أوّل نسخة مستقرة
      (`src/lib/version.ts` مصدرٌ واحد، `npm run bump` يرفعه، و
      `scripts/check-version.mjs` بوابةٌ قبل البناء تمنع انحراف `package.json`).

## المساعد (أُزيل)

- صفحة المحادثة `/assistant` ووسيطُها لم يعودا في التطبيق (حُذفا بقرار المالك).
      بقي منه `assistantContext.ts` لأنّ `buildDayDigest` وحدها تُستعمل في
      «خلاصة اليوم» بقسم قرآن — لا يوجد أيّ نداء لنموذجٍ خارجيّ في التطبيق.

## المذكرات والقراءة

- [x] تجربة تحرير غنية (تنسيق، حفظ تلقائي، مسودّة، صور متعددة، صوت)، شريط سنوات،
      مفضلة، تنقّل، ذكرى عشوائية، معرض، و«في مثل هذا اليوم».
- [x] **وسوم للمذكرات**: إدخالها في النموذج وفلترة بضغطة على شريط الوسوم وعرضها في
      القارئ.
- [x] تعديل/حذف جلسات القراءة (لا يُعاد احتساب `currentPage`).
- [x] **مؤقّت جلسة قراءة** يملأ الدقائق تلقائياً، و**هدف قراءة سنوي** مع مؤشر وتيرة.

## لمسات واجهة

- [x] انتقال ناعم بين الصفحات (`PageTransition`)، مؤشر منزلق في الشريط السفلي،
      هيدر/شريط زجاجي (`backdrop-blur`).
- [x] تحوّل تدريجي للألوان عند انقلاب الوضع الليلي (الغروب/الشروق أو التبديل اليدوي).
- [x] احتفالات المعالم (سلسلة 7/30/100…، إنهاء الكتب) بكونفيتي مرة واحدة لكل معلم.
- [x] رسوم الحالات الفارغة بطابع أندلسي (نجمة ثمانية).
- [x] مشاركة «حصيلة الأسبوع» كصورة PNG (canvas، بلا مكتبات).

## خصوصية

- [x] قفل رمز محلي (PIN من 4 أرقام، SHA-256، لا يُزامن) يُطلب عند فتح التطبيق —
      يُفعّل/يُلغى من الإعدادات (`LockCard`).
- [x] شارة على أيقونة التطبيق (Badging API) حين يكون اليوم ناقصاً.

## أعراف مثبّتة (انظر CLAUDE.md)

- تواريخ محلية `YYYY-MM-DD` عبر `toDateStr/parseDate/today` (لا `toISOString`).
- أرقام العرض المالية عبر `formatAmount`؛ حساب مالي مُقرَّب بـ`round2`؛ ميزانية يومية
      تراكمية مع `carryAdjust`.
- الوضع الليلي مركزي في `globals.css`؛ التنقّل من مصدر واحد `lib/nav.ts`.
- عدّاد السلسلة (`calcStreak`) لم يعد محدوداً بسنة — يُحسب حتى أول انقطاع فعلي.
- «اليوم المكتمل» تعريفٌ **واحد** في `dayAggregator.ts` (مذكرة · قراءة · وِرد،
      ويُستثنى المجمّد) يقوده التقويم والإحصائيات وشارة اليوم والمعالم معاً.

## سلامة المزامنة بين الأجهزة (جولة ما قبل التجميد)

- [x] **الستةُ كلُّها لكلّ حقل** (`snapshot` · `hydrate` · `normalizeBackup` ·
      `mergeAppData` · `hasData` · `cloudHasUnseen`): كانت `assets` في خمسةٍ
      وغائبةً عن `hydrate`، فلم يستقبلها جهازٌ جديد من السحابة ولا استعادةٌ من
      ملف. حُذفت الأصولُ لاحقاً والدرسُ باقٍ — وحارسُ round-trip يغطّي **كلّ**
      حقول `AppData`.
- [x] **طابعُ تعديلٍ لكلّ عنصر** (الكتب · جلسات القراءة · التأمّلات · الصناديق ·
      التصنيفات · العادات · الرسائل · السقوف · قواعد التجار · حالة الصلاة ·
      الختمة · ترتيب التصنيفات): تعديلُ عنصرٍ قائم على جهاز لم يعد يخسر لأنّ ختم
      مستند الجهاز الآخر أحدث إجمالاً (كان تقدّم القراءة يرجع للخلف).
- [x] النسخة الاحتياطية لم تعد تُسقط `frozenHabits` عند الاستعادة.
- [x] جهازٌ جديد يتبنّى السحابة كاملةً يمرّ بـ`applyTombstones` أولاً، فلا تعود
      مذكرةٌ محذوفة من shard قديم لم يُحذف.
- [x] **العناصر المركّبة**: للمجموعة الداخلية دمجُها وشواهدُها، ولبيانات العنصر
      طابعُها — فسجلّاتُ العادة وإيداعاتُ الصندوق ووسائطُ المذكرة لا ترفع الطابع.
- [x] ختمُ الصلاة لكلّ `date + prayer`، وطابعٌ مستقلّ لـ`dailyPageGoal`.
- [x] **ترتيب المزامنة**: الدمج يقع على مراجع الوسائط (`photoRefs`/`audioRefs`)
      لا على البايتات، والترطيب بعده للعرض وحده، والمحفوظ هو الناتج الغنيّ
      بالمراجع — فلا تتيتّم صورةٌ أضافها الجهاز الآخر. والجهاز الجديد ينقّي
      بالشواهد قبل التنزيل.
- [x] حارسا الدورة والنسخة الاحتياطية بنوع `Required<AppData>`: الحقلُ الاختياريّ
      الجديد يكسر الترجمة بدل أن ينزلق صامتاً.

## مؤجَّل بقرار المالك

- README عام، وتصدير مصاريف CSV. (اختبارات Vitest وCI صارت قائمة.)
