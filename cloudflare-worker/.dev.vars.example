# Never commit .dev.vars. Copy this file to .dev.vars for local development.
# The Worker reads/writes R2 through its binding (MEDIA_BUCKET), so no S3
# account id / access key / secret is needed anymore — only the sync-key hash.
SYNC_KEY_SHA256=replace-with-lowercase-sha256-of-your-sync-key
