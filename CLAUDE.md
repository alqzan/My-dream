# CLAUDE.md — دليل العمل في هذا المستودع (مدار)

## سير العمل — أين تكتب التعديلات (الأهم)

**اكتب مباشرةً على `main` وادفع إليه.** هذا مشروع شخصي بمستخدم واحد، والمالك
يريد أن تصل التعديلات مباشرةً للنشر. GitHub Pages ينشر **من `main` فقط**
(`.github/workflows/pages.yml`)، فأي عمل خارج `main` لا يُنشر ولا يظهر.

- **لا تُنشئ فروعاً (branches) ولا Pull Requests** إلا إذا طلب المالك مراجعةً
  صراحةً. دورة الفرع → PR كانت تسبّب تعارضات متكرّرة لأن `main` يتحرّك بين الجلسات.
- **زامِن قبل أن تبدأ وقبل أن تدفع:** `git pull --rebase origin main`. هكذا يبقى
  كل تعديل فوق أحدث `main` ولا تحدث تعارضات أصلاً.
- **شغّل البوابة قبل الدفع:** `npm test` ثمّ `npm run lint` ثمّ `npm run build`.
  **يوجد CI**: `.github/workflows/pages.yml` يشغّل الاختبارات وESLint وفحص أنواع
  الـWorker والبناء قبل النشر، وأيّ أحمر يمنع وصول النسخة للموقع الحيّ — فتشغيلها
  محلياً يوفّر دورةَ فشلٍ كاملة.
- بعد الدفع إلى `main`، ينشر workflow الصفحات تلقائياً.
- **إن ألزمتك أدوات الجلسة بفرع عمل رغم ذلك:** أعِد التأسيس على أحدث `main`
  قبل الدفع وأبقِ الفرع قابلاً للدمج فوراً (لا تدع `main` يتقدّم عليه ويتعارض).

## نبذة عن المشروع

- تطبيق PWA شخصي عربي (RTL) اسمه **مدار** لتتبّع: المصاريف، المذكرات، الصلوات،
  القراءة، العادات، والإحصائيات.
- **التقنيات:** Next.js 15 (App Router، `output: "export"` → موقع ثابت)، React 18،
  Zustand (محفوظ في IndexedDB عبر idb-keyval)، Tailwind، Firebase/Firestore
  لمزامنة سحابية اختيارية (نموذج «login-free»: `src/components/sync/`).
- كل الحالة على جهاز المستخدم؛ Firestore للمزامنة فقط.

## الأوامر

- `npm run dev` — خادم تطوير محلي.
- `npm run build` — إخراج ثابت للإنتاج في `out/` (يفحص الأنواع أيضاً، ويتحقّق من
  الإصدار أولاً عبر `scripts/check-version.mjs`).
- `npm test` — اختبارات Vitest. `npm run lint` — ESLint.
- `npm run bump` — **يرفع رقم الإصدار تعديلاً واحداً**: شغّله مع كل تعديلٍ جوهريّ
  (ميزة/إصلاح/مهاجرة) قبل الدفع، وسجّل السطر المقابل في `ROADMAP.md`.
- النشر: ادفع إلى `main` (تلقائي عبر GitHub Pages).

## الإصدار (قاعدة ثابتة)

رقم الإصدار يعكس **عدد التعديلات الجوهرية منذ أوّل نسخة مستقرة**، لا إصداراتٍ
تسويقية: `APP_BUILD` في `src/lib/version.ts` هو المصدر الوحيد، و`APP_VERSION` =
`0.1.<build>` وهو ما في `package.json` وما تعرضه صفحة الإعدادات. تسلسليٌّ دائماً
(+1)، **بلا قفزٍ وبلا إعادة تعيين**. لا تكتب الرقم بيدك في مكانين — استخدم
`npm run bump` (والبناء يكسر عند أيّ انحراف).

## أعراف وأفخاخ (حافظ على الاتساق)

- **التواريخ:** دائماً مفاتيح محلية `YYYY-MM-DD` عبر
  `toDateStr`/`parseDate`/`today` في `src/lib/utils.ts`. لا تستخدم
  `toISOString()` لمفاتيح التاريخ (UTC يزيح اليوم في الخليج).
- **الأرقام — هندية في كل مكان (قرار المالك، ٠٫١٫٣٥٨):** المبالغ والأعداد
  والتواريخ والمواقيت كلّها **هندية**، والمبالغُ منها. نسّق المبالغ بـ
  `formatAmount`، والأعداد بـ`arabicCount`، وما لا يمرّ بمنسّق `Intl` مرِّره
  على `toIndicDigits` — وهي **البوّابة الوحيدة** للتحويل.
  - **ثبّت نظام الأرقام صراحةً** (`-u-nu-arab`) في أي منسّق `Intl` جديد؛ لا
    تتركه لافتراض `ar-SA`. التثبيت هو ما يمنع تعارض الترطيب بين البناء والعميل
    (كانت `-u-nu-latn` مثبّتةً للسبب نفسه قبل أن ينقلب القرار).
  - **لا تحوّل قيمةً تُخزَّن أو تُقارَن**: مفاتيح التواريخ (`YYYY-MM-DD`)
    والمعرّفات تبقى لاتينية. التحويل للعرض وحده.
  - الحارس: `src/lib/indicDigits.test.ts` — منسّقٌ واحد يُترك لاتينياً يُظهر رقماً
    شاذّاً وسط شاشةٍ هندية، وهو الخلط الذي رفضه المالك.
- **المال:** قرِّب عند الحدود بـ`round2`؛ الميزانية اليومية تراكمية
  (`computeDailyBudgetStatus`، مع `carryAdjust` بعد ترحيل الفائض/الراتب).
- **RTL:** الشريط الجانبي الثابت يستخدم `right-0`/`border-l`/`mr-56` الفيزيائية
  عمداً؛ فيما عدا ذلك فضّل الأصناف المنطقية (`ms-`/`me-`/`start`/`end`).
- **الوضع الليلي:** يُقاد بصنف `.dark`؛ خلفيات `bg-*-50/100` الباهتة مُعاد
  تعيينها في `globals.css` — عند إضافة سطح ملوّن جديد أضِف تعيينه الليلي هناك.
- **التنقّل:** المصدر الوحيد هو `src/lib/nav.ts` (يشترك فيه `Sidebar` و`MobileNav`)
  — عدّل المسمّيات هناك.
- **تعديلات المتجر** تختم `lastUpdated` تلقائياً (غلاف `set` في
  `src/lib/store.ts`)؛ استخدم `rawSet` فقط للكتابة المحلية بالجهاز/الترطيب.
- **«اليوم المكتمل»** تعريفٌ واحد في `src/lib/dayAggregator.ts`
  (`dayRitualStates`/`completedDayDates`): مذكرة · قراءة · وِرد قرآني، ويُستثنى
  المجمّد. لا تُنشئ نسخةً ثانية منه في صفحةٍ أو مكوّن.
- **أبوابٌ محذوفةٌ بقرار المالك (٠٫١٫٣٨٧) — لا تُعِدها بلا طلبٍ صريح**: الأصول
  والإهلاك · خطط الأقساط · الالتزامات المتكرّرة · الرفّ · بوصلة التوصيات. حُذفت
  لأنّها كانت تصف التزاماً أو تعرض حساباً ولا تصرف ريالاً — والمالك أرادها تروح
  لا أن تُخفى. المتبقّي في المال ثلاثة: البدل اليومي · السقوف · الاحتياطيات.
  - **الحذفُ تفريغُ أرضٍ لا منعٌ دائم**: نيّةُ المالك أن يُعيد ترتيب ما يحتاجه
    منها لاحقاً بشكلٍ أنظف. فإن طلب ذلك صراحةً فابنِ الجديد من الصفر على حاجته
    وقتَها، ولا تُعِد القديم كما كان: نصُّه كلُّه محفوظٌ في تاريخ git عند
    `0d860ea` مرجعاً يُقرأ، لا قالباً يُنسَخ. وبياناتُه السابقة لا تعيش إلا في
    نسخةٍ احتياطية صدّرها المالك قبل الحذف — فاسأله عنها قبل أن تفترض ضياعها.
- **الصرف النقديّ**: أيّ تجميعٍ للصرف يمرّ بـ`cashOut(t)` من `utils.ts` — **لا
  تجمع `t.amount` مباشرةً**. صارت الدالّة اليوم مطابِقةً للمبلغ (لم يبقَ نوعٌ من
  المعاملات لا يخرج نقداً بعد حذف الأقساط)، وتبقى البوّابةَ الوحيدة: هي المكان
  الذي يتغيّر لو عاد مفهوم «التزامٌ لم يُدفع»، بدل مطاردة عشرات المجاميع.
- **حقلٌ جديد في `AppData` يجب أن يُغطّى في الستة كلّها**: `snapshot` و`hydrate`
  (كلاهما في `store.ts`) و`normalizeBackup` (`BackupCard.tsx`) و`mergeAppData`
  (`merge.ts`) و`hasData` و`cloudHasUnseen` (`syncDecision.ts`). إغفال أيٍّ منها
  عطلٌ صامت: `assets` كانت في خمسةٍ وغائبةً عن `hydrate`، فلم يستقبل جهازٌ جديد
  أصلاً من السحابة ولا استعادةٌ من ملف (حُذفت الأصول لاحقاً، والدرسُ باقٍ).
  الحارس في `store.roundtrip.test.ts`.
- **أختام التعديل لكلّ عنصر**: غلاف `set` يختم `updatedAt` تلقائياً على أيّ عنصرٍ
  مفتاحُه `id` تغيّر (وعلى يوم الصلاة والسقف)، والدمج يحسم تعارض العنصر بطابعه هو
  (`byIdNewer`/`byKeyNewer`) لا بختم المستند. لا تختمه يدوياً في الإجراءات، ولا
  تدع حقلاً آلياً أو مجموعةً داخليةً لها دمجُها المستقل
  (`habits.logs` · `reserves.deposits` · وسائط المذكرة) يبدو تعديلاً للعنصر —
  سجّله في `UNSTAMPED_FIELDS`. حالةُ الصلاة تُختم لكلّ `date + prayer`، وهدفُ
  الختمة اليومي بطابعٍ مستقلٍّ عن تقدّمها.
- **ترتيب المزامنة ثابت**: ادمج مع لقطة السحابة **وهي تحمل مراجع الوسائط**
  (`mergeAppData(local, cloudMain)`)، ثمّ `hydrateCloudPhotos` على **الناتج**
  للعرض، واحفظ الناتج الغنيّ بالمراجع لا نسخة العرض. ترطيبٌ قبل الدمج يحوّل
  المراجع بايتاتٍ فتسقط وسائطُ الجهاز الآخر بلا مرجعٍ يعيدها (يتيمة في R2).
  وعلى مسار الجهاز الجديد: `applyTombstones` **قبل** الترطيب.
- **قرار تبنّي لقطة المزامنة** في `src/lib/syncDecision.ts` (نقيّ ومختبَر):
  ختمٌ أحدث **أو** ارتفاع `revision` **أو** محتوى لم نره. لا تُعِد هذا الشرط داخل
  المكوّن.

## جاهزية App Store (النقل إلى Capacitor لاحقاً)

- **منطق المجال يبقى مستقلاً**: الحساب النقيّ في `src/lib/*.ts` بلا `window` ولا
  DOM ولا Firebase — هكذا يعبر إلى الغلاف الأصليّ بلا تعديل ويبقى مختبَراً.
- **كل ما هو منصّة خلف واجهةٍ قابلة للاستبدال**: التخزين (`idbStorage.ts`)،
  الوسائط والصور (`imageUtils.ts`/`mediaHash.ts`)، المشاركة والملفات (التصدير
  والاستيراد في `BackupCard.tsx`)، الأذونات والإشعارات. لا تنثر `navigator.*`
  ولا `document.*` في الصفحات — نادِ الواجهة، فيُبدَّل تنفيذُها بالمكوّن الأصليّ.
- **لا تقسّم `store.ts`/`sync.ts` لمجرّد النظافة**؛ افصل عند النقل الطبقاتِ التي
  يحتاجها Capacitor فقط.
- **الخطة الكاملة في `docs/APP-STORE-PLAN.md`** (المسار المعتمد، قائمة ما ينكسر
  بالملفات والأسطر، الأذونات، والتحديث الحيّ). **وهي وثيقة حيّة**: أيّ تعديلٍ
  يضيف استخدام `navigator.*`/`document.*` خارج المكوّنات، أو مفتاح `localStorage`
  جديداً، أو تنزيلَ ملفٍ ومشاركةً، أو إذناً جديداً — يحدّث القسم المقابل هناك
  **في نفس الـcommit** (القاعدة مفصّلة في القسم 8 من الملف).

## مفتاح المزامنة (سري — ليس في الكود)

معرّف مساحة المزامنة (`SYNC_SPACE_ID` سابقاً) لم يعد قيمة ثابتة في
`src/lib/firebase.ts` — كان مكشوفاً نصاً في مستودع عام. النموذج الآن:
`getSyncSpace()` في `src/lib/firebase.ts` تُرجع `NEXT_PUBLIC_SYNC_SPACE` إن
وُجد (بناء خاص)، وإلا تقرأ `localStorage["madar-sync-space"]` (بحارس
`typeof window`) — تُكتب مرة واحدة لكل جهاز عبر بطاقة «مفتاح المزامنة»
(`src/components/settings/SyncKeyCard.tsx`) ثم `location.reload()`.
كل استخدام لمعرّف المساحة (`sync.ts`, `SyncProvider`, `DayOneImport`,
`PendingInboxWatcher`) يستدعي `getSyncSpace()` ويتعطّل بهدوء (لا يزامن) عند
غيابه. `firestore.rules` و`storage.rules` في المستودع تحمل قيمة شكلية
(`REPLACED_IN_FIREBASE_CONSOLE`) — القواعد الفعلية تُنشر من Firebase Console
حصراً ولا تُكتب هنا أبداً.
