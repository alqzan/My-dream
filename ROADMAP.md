# خارطة الطريق الموحدة — «مدار»

> نتيجة تفنيد تقرير المراجعة الشاملة (22 يوليو 2026، نسخة `cdb4dd8` / `local-media-16`)
> مقابل الكود الفعلي، سطراً سطراً. **اعتمدها المالك رسمياً بدل توصيات التقرير
> الأول** — هذه هي الوثيقة المرجعية الوحيدة لترتيب العمل القادم؛ عند إنجاز
> بند يُشطب هنا في نفس الـcommit.

## خلاصة التفنيد

التقرير **دقيق تقنياً في تشخيصه** — كل بند حرج تحققنا منه في الكود وثبت
(المراجع أدناه). لكنه **مبالغ في علاجاته وأولوياته** في أربعة مواضع:

1. **CRDT/سجل أوامر/طابور تعديلات** (قسم 7 من التقرير): تعقيد لا يتناسب مع
   مستخدم واحد و2–3 أجهزة. البديل: تعميم النمط الموجود أصلاً في `merge.ts`
   (tombstones + `updatedAt` لكل عنصر) على الكيانات المتبقية، وقيم مفردة
   بصيغة `{ value, updatedAt }`. هذه **ضمانات كافية لحالة استخدام مدار**
   بخُمس الكلفة — لا تساوي ضمانات CRDT رياضياً (تعتمد على ساعات الأجهزة)،
   لكن انحراف الساعات بين أجهزة مالك واحد مقبول ضمن هذا النموذج.
2. **نقل الأصل (origin) كـP0**: صحيح نظرياً أن `alqzan.github.io` أصلٌ مشترك،
   لكن الخطر يتطلب وجود مشروع آخر مخترق على نفس الأصل — احتمال ضئيل لحساب
   شخصي. والتقرير أغفل الكلفة الحقيقية: النقل **يفقد IndexedDB/localStorage
   والتثبيت الحالي** ويستلزم إعادة إدخال مفتاح المزامنة على كل جهاز. → مؤجل (مرحلة 5).
3. **WCAG 2.2 AA كامل + صفر مخالفات axe**: ترفٌ لتطبيق مستخدمه الوحيد هو
   مالكه. يُؤخذ منه ما يمس الاستخدام الفعلي فقط: أهداف اللمس، الخطوط 8–11px،
   تباين الداكن، التركيز الأول في النماذج.
4. **مستورد Day One الدفعي كـP0**: التشخيص مؤكد (`dayOneParser.ts` يحتفظ بكل
   الوسائط في `photoData`/`audioData` Maps في الذاكرة)، لكن أولويته **مشروطة
   بوجود استيراد ضخم قادم فعلاً**. إن لم يكن، فهو مرحلة متأخرة.

ما عدا ذلك، البنود الحرجة مؤكدة ومقبولة كما هي.

## ما ثبت في الكود (الأدلة)

| البند | الدليل | الحكم |
|---|---|---|
| إلغاء علامة عادة/وِرد يرجع من جهاز آخر | `merge.ts:82-86` (اتحاد logs)، `:185` (quranWird) | ✅ مؤكد |
| حذف وديعة احتياطي يرجع | `store.ts:582` يحذف بلا tombstone، و`merge.ts:89-93` يتحد بالـid | ✅ مؤكد |
| حذف ميزانية تصنيف يرجع | `store.ts:501` بلا tombstone، و`merge.ts:177` يتحد بالـcategory | ✅ مؤكد |
| مسح قيمة مفردة لا يتزامن | `merge.ts:190-195` (`??` يفضّل non-null) | ✅ مؤكد |
| `hasData` لا يعدّ القرآن/التصنيفات/الإعدادات | كان `SyncProvider.tsx:51-64` (الآن `syncDecision.ts`) | ✅ مؤكد ومُصلَح |
| `cloudHasUnseen` يراقب 6 مجموعات فقط | كان `SyncProvider.tsx:71-85` (الآن `syncDecision.ts`) | ✅ مؤكد ومُصلَح |
| النسخة الاحتياطية تسقط `deletedMedia` | `BackupCard.tsx` — `normalizeBackup` (يحفظ الآن الشواهد والأقساط) | ✅ مؤكد ومُصلَح |
| read–merge–write غير ذرية | `SyncProvider.tsx:215-240` — لكن يوجد تخفيف (إعادة قراءة + دمج قبل الدفع) | ✅ مؤكد جزئياً |
| CI لا يشغّل الاختبارات | `pages.yml` — `npm ci` ثم build فقط | ✅ مؤكد |
| لا precache وقفل portrait | `sw.js` (80 سطراً)، `manifest.webmanifest:10` | ✅ مؤكد |
| لا إعداد ESLint | لا يوجد `eslint.config.*` في الجذر | ✅ مؤكد |

## الخطة — خمس مراحل مرتبة

### المرحلة 1 — سد ثغرات فقد/رجوع البيانات (الأولوية القصوى، أثر كبير بكلفة صغيرة)

> الحالة: **منجزة بالكامل** (51 اختباراً أخضر، بناء أخضر).

1. ✅ **`hasData` و`cloudHasUnseen` مشتقتان من كل `AppData`** بدل القوائم اليدوية —
   الآن تغطيان القرآن والحفظ والوِرد والتصنيفات والقيم المفردة وشواهد الحذف،
   مع استبعاد المصنّفات الافتراضية والعادات المزروعة (وإلا بدا كل جهاز «غير فارغ»).
   `cloudHasUnseen` تراقب أيضاً التواريخ والشواهد فينتشر الحذف حياً.
   (`SyncProvider.tsx`)
2. **تعميم نمط الحذف الموجود** على الكيانات المتبقية (بلا CRDT) — مفاتيح شواهد
   منفصلة النطاق في نفس خريطة `deleted` (`merge.ts`: `budgetTombKey`,
   `depositTombKey`, `habitLogTombKey`, `wirdTombKey`)، يكتبها المتجر عند
   الحذف/الإلغاء ويرفعها عند إعادة الإضافة، ويُرشّحها الدمج:
   - ✅ ميزانيات التصنيفات: `budget:<category>` (`store.ts` removeBudget/setBudget).
   - ✅ ودائع الاحتياطي: `deposit:<id>` (deleteReserveDeposit/addReserveDeposit).
   - ✅ سجلات العادات والوِرد: `habitlog:<id>:<date>` و`wird:<date>`
     (toggleHabitLog/toggleWird).
   - ✅ **القيم المفردة** (`dailyBudget`, `monthlyIncome`, `readingGoal`,
     `salaryDay`, `lastSalaryConfirm`, `frozenHabits`): خريطة طوابع
     `fieldUpdatedAt` بجانب الحقول (لا تغيير في شكلها ولا في القرّاء) — يختمها
     غلاف `set` تلقائياً عند أي ضبط، ويختار الدمج «آخر ضبطٍ يفوز» فيسري المسح
     إلى `null`؛ عند غياب الطوابع (بيانات قديمة) يرجع للسلوك السابق (non-null)
     فلا يتراجع شيء. محفوظة في النسخة الاحتياطية أيضاً. (`types.ts` · `store.ts`
     · `merge.ts` · `BackupCard.tsx`)
3. ✅ **النسخة الاحتياطية** (`BackupCard.tsx`): `deletedMedia` محفوظة في
   `normalizeBackup` والتصدير؛ الدمج يوحّد الشواهد بأحدث طابع لكل مفتاح مركّب
   `entryId:kind:hash` (لا استبدال)؛ «الاستبدال» عند فشل checksum يتطلب تأكيداً
   ثانياً صريحاً؛ عدّ الوسائط لا يضاعف أول عنصر (`photos[0]` = `photo`).
4. ✅ **اختبارات merge للحالات الجديدة** في `merge.test.ts`: حذف ميزانية، حذف
   وديعة، إلغاء إكمال عادة، إلغاء وِرد، وإعادة إضافة بعد رفع الشاهد — لا رجوع
   لحذف ولا ضياع لتعديل.

**بوابة الخروج:** ✅ كل سيناريوهات الحذف/الإلغاء ومسح القيم المفردة مغطاة باختبار أخضر.

### المرحلة 2 — صلابة المزامنة

> الحالة: **منجزة بالكامل** (54 اختباراً أخضر، بناء أخضر).

1. ✅ حقل `revision` رقمي في المستند الرئيسي + **معاملة Firestore** عند الكتابة
   (compare-and-set): `saveUserData` يكتب المستند الرئيسي داخل `runTransaction`
   يرفع `revision`؛ إن تقدّم عن `expectedRevision` يرمي `RevisionConflictError`،
   فيعيد `pushLocal` القراءة والدمج والمحاولة (محدودة بأربع مرّات فلا تدور).
   **نطاقه محدد بوضوح**: يمنع الكتابة فوق نسخة أحدث في المستند الرئيسي فقط —
   لا يجعل المزامنة ذرية عبر shards اليوميات أو الوسائط؛ تلك تغطيها حالة «جزئي»
   وإعادة المحاولة. (`sync.ts` · `SyncProvider.tsx`)
2. ✅ حالة مزامنة صادقة: `lastShardLoadOk()` يرصد فشل قراءة الشرائح، فتظهر حالة
   `"partial"` («مزامنة جزئية») بدل ادّعاء «متزامن». (`sync.ts` · `SyncProvider.tsx`
   · `SyncStatus.tsx`)
3. ✅ رفع نتيجة الدمج إلى السحابة بعد تبني snapshot بعيد إذا حمل الجهاز تغييراً
   لا تعرفه السحابة (`cloudHasUnseen(local, cloudMain)`) — مع حارس ضدّ الحلقة
   (لا يدفع جهازان خاملان ذهاباً وإياباً). كان عنصر محلي قد يبقى على جهاز حتى
   تعديلٍ لاحق. (`SyncProvider.tsx`)
4. ✅ رفع مهلة tombstones من 120 يوماً إلى **سنة** (`merge.ts`). **قرار متعمد
   وموثق**: جهاز لم يتصل لأكثر من سنة قد يعيد بيانات قديمة — مقبول لأجهزة مالك
   واحد؛ العلاج حينها مسح بيانات الجهاز العائد وتبني السحابة، لا watermark إضافي.

**بوابة الخروج:** ✅ CAS يمنع فقد التحديث على المستند الرئيسي (اختبار تعارض أخضر)،
والحالة الجزئية صادقة، والدمج يتقارب على الأجهزة. يبقى الاختبار الميداني على
جهازين فعليين (يوصى به قبل الاعتماد الكامل، ليس بواباً برمجياً).

#### ✅ تكملة المرحلة 2 — ثلاث ثغرات سلامة أُغلقت (الإصدار 0.1.292)

1. ✅ **قرار تبنّي لقطة السحابة صار يعتبر `revision`** (`syncDecision.ts` جديد).
   كان الشرط في المستمع الحيّ معلّقاً على أمرين فقط: ختمٌ أحدث، أو محتوى «لم نره»
   (معرّف/تاريخ/شاهد جديد). فكان **تعديلُ عنصرٍ قائم** على جهازٍ ساعته متأخّرة
   يُتجاهَل بلا أثر: لا معرّف جديد، ولا شاهد جديد، والختم يبدو أقدم. الآن ارتفاع
   `cloudMain.revision` عن آخر revision تبنّيناه سببٌ كافٍ للدمج — وهو عدّادٌ
   يرفعه كلّ حفظٍ في معاملة Firestore فلا يعتمد على ساعة أيّ جهاز.
   المنطق خرج من المكوّن إلى دوالٍّ نقيّة مختبَرة: `decideAdoptCloud` /
   `shouldAdoptCloud` (مع `hasData` و`cloudHasUnseen` المنقولتين معها) — 9 اختبارات
   تشمل الحالة بالضبط: **لا عناصر جديدة، لا شواهد جديدة، ختمٌ أقدم، revision أعلى،
   وتعديلٌ قائمٌ يجب دمجه**.
2. ✅ **دلالات الالتزامات المتكرّرة** (`merge.ts` · `store.ts` · `utils.ts`):
   - `updatedAt` لكل قاعدة (تُختم على الإضافة/التعديل لا على التوليد الآلي، فلا
     يطغى توليدٌ على تعديلٍ حقيقيّ من الجهاز الآخر)، متوافقٌ مع القديم (غيابه = 0).
   - `mergeRecurringRules`: آخر تعديلٍ يفوز بالحقول، و**`lastGenerated` لا يرجع
     للخلف** أبداً (الأحدث تاريخياً من الجهازين) — كان الرجوع يُعيد توليد قسط شهرٍ
     ولّده الجهاز الآخر.
   - `runRecurring` **idempotent**: يتحقّق من عدم وجود المعرّف الحتميّ
     (`rec_<rule>_<date>`) في `transactions` قبل الإضافة، فتشغيلان متتاليان أو
     معاملةٌ وصلت بالمزامنة لا يُنتِجان صفّاً مكرّراً. و`lastGenerated` يتقدّم حتى
     لو تُخطّي كل المواعيد كمكرّرة (فلا يُعاد الفحص).
   - `generationMode` غير الموجود = `"auto"` عبر `generationModeOf` (مصدرٌ واحد
     للافتراض)، و`"reminder"` لا يولّد معاملةً أبداً.
3. ✅ **تعريفٌ واحد لـ«اليوم المكتمل»** (`dayAggregator.ts`): `CORE_RITUALS` ·
   `dayRitualStates` · `isDayComplete` · `completedDayDates` · `activeRitualLabels`.
   كانت ثلاثة تعريفات متباعدة: `aggregateDay` (مذكرة + قراءة + وِرد، يحترم
   التجميد)، و`getDailyCompletionDates` في `utils.ts` (مذكرة + قراءة فقط) تقود
   تقويم الرئيسية والمعالم، وثالثٌ داخل `stats/page.tsx` (مذكرة + قراءة فقط).
   الآن الجميع يقرأ من الدالة المركزية، وأُزيلت `getDailyCompletionDates`.
   **أثرٌ مقصود على المالك**: السلسلة صارت تشترط نشاطاً قرآنياً في اليوم (وِرد أو
   حفظ أو مراجعة أو تدبّر أو تقدّم ختمة)، فقد ينقص رقمُ السلسلة الظاهر — وهذا هو
   الرقم الصادق الذي تعرضه شارة اليوم أصلاً. تجميد «الوِرد» يُخرجه من الشرط،
   وعنوان بطاقة السلسلة يتبدّل مع الطقوس النشطة فلا يكذب.

### المرحلة 3 — وقاية وبوابة جودة

> الحالة: **البوابة البرمجية منجزة**؛ يبقى بندان يحتاجان لوحة Cloudflare/Firebase.

1. ✅ **CI**: `pages.yml` صار يشغّل `npm test` ثم `npm run lint` ثم typecheck
   الـWorker قبل build (الأرخص أولاً). فشل أيٍّ منها يمنع النشر — و`main` يبقى
   مصدر النشر الوحيد. (`.github/workflows/pages.yml`)
2. ✅ **ESLint**: `.eslintrc.json` (`next/core-web-vitals`) يعمل بلا مطالبة
   تفاعلية؛ أُصلح خطآ الاقتباس في `ReadingLogForm.tsx` (« » بدل " ")؛ يمرّ
   أخضر (تبقى تحذيرات `<img>`/deps غير حاجزة، مقصودة لصور `data:`).
3. ◑ **مفتاح المزامنة**: ✅ زر «توليد مفتاح قوي» (160 بت من CSPRNG) + نُصح غير
   حاجز عند مفتاح ضعيف + إظهار/إخفاء الإدخال للنسخ قبل الحفظ (`SyncKeyCard.tsx`).
   ⏳ **rate limiting على الـWorker مؤجّل**: يتطلب ربط Rate-Limiting/KV من لوحة
   Cloudflare (لا يمكن سنّه من الكود وحده دون كسر البوابة الحيّة)، والمصادقة
   الحالية HMAC ثابت الزمن بمفتاح 160 بت تجعل التخمين غير عملي أصلاً. الخطوات
   موثّقة في `cloudflare-worker/README.md`.
4. ✅ **تحديثات الاعتماديات الآمنة**: `npm update` داخل نطاقات `^` (firebase
   12.16، next 15.5.21، postcss، …) بتحقّق أخضر. الترقيات الكبرى (React 19،
   Next 16، recharts 3، tailwind 4) تبقى مؤجّلة لجلسة مركّزة.
5. ⏳ **قواعد Firestore الإنتاجية**: تُراجَع من Firebase Console (لا يمكن من هنا)؛
   يبقى `firestore.rules` في المستودع قالباً شكلياً، والقواعد الفعلية تُنشر من
   الـConsole حصراً كما في `CLAUDE.md`.

### المرحلة 4 — التجربة اليومية (منجزة، الأثر المباشر على المالك)

> الحالة: **منجزة** (بناء + lint + 54 اختباراً أخضر)، مع بندين تفضيليين مؤجّلين للمالك.

1. ◑ **الجوال**: ✅ أهداف لمس ≥44px لتبويبات الشريط السفلي + تسميات 10.5px
   (shortLabel للأطول). ⏳ إعادة هيكلة الشريط إلى 4–5 وجهات + «المزيد» مؤجّلة
   كقرار تفضيلٍ للمالك (الخارطة تصفها اختياراً). (`MobileNav.tsx` · `nav.ts`)
2. ✅ **الداكن**: رفع `#897c63`→`#9d8f74` وتركوازي الصلاة وأخضر المال كنصٍّ
   إلى ≥4.5:1 مع حفظ الهرمية. (`globals.css`)
3. ✅ **النماذج**: autoFocus على المبلغ في المصروف السريع؛ labels/aria للبحث
   وعنوان/نص المذكرة وكلمتي مرور النسخة ومفتاح المزامنة.
4. ✅ **PWA**: `scripts/gen-precache.mjs` يولّد `precache.json` بكل المسارات
   والأصول ويختم build id في `sw.js`؛ التثبيت يخزّن الكل (تعمل صفحةٌ لم تُزَر
   دون اتصال)؛ كل كتابة كاش مربوطة بـ`waitUntil`؛ إزالة قفل `portrait`.
5. ◑ **سطح المكتب**: ✅ توسيع لطيف إلى `max-w-3xl` على `xl` لصفحات اللوحات مع
   إبقاء اليوميات/القرآن/القراءة ضيّقة. ⏳ إعادة التخطيط الكامل بعمودين مؤجّلة
   لمعاينة المالك (قرار ذوقي — لا يُنفَّذ أعمى).
6. ✅ `DataHealthCard`: حجم المستند الرئيسي وأكبر shard شهري كلٌّ على حدة (بدل
   مجموع مضلل)؛ `BUILD_TAG`→`NEXT_PUBLIC_BUILD_TAG` (commit SHA من CI).

### المرحلة 5 — تنظيف وصيانة (جارية)

- ✅ إزالة ركام S3/`aws4fetch`: حذف تبعية `aws4fetch` غير المستخدمة من الـWorker
  (+ تحديث القفل)، وتقليم متغيرات S3 من `.dev.vars.example`. تصحيح README الـWorker
  للمسارات الفعلية سبق في المرحلة 3.
- ⏳ توحيد مصدر الـWorker (إزالة نسخة `worker.dashboard.js` اليدوية ببناء واحد) —
  مؤجّل لأن المالك يلصق `worker.dashboard.js` يدوياً في لوحة Cloudflare.
- ✅ **Day One الدفعي القابل للاستئناف**: `streamDayOneZipImport` يُدفّق الأرشيف
  ويُخرج المذكرات دفعاتٍ (٤٠) مع تحرير وسائط كل دفعة فوراً — فتبقى الذاكرة قرب
  دفعة واحدة لا الأرشيف كله؛ زر إيقاف، شريط تقدّم، واستئناف تلقائي (المتجر يزيل
  التكرار بالمعرّف الثابت). ٣ اختبارات (دفعات/إلغاء/backpressure). حُذف
  `parseDayOneZip` القديم فلا يبقى مساران متضاربان. (`dayOneParser.ts` · `DayOneImport.tsx`)
- ⏳ **أصل مستقل** (نطاق/Cloudflare Pages): مع خطة انتقال — نسخة احتياطية متحققة،
  إعادة إدخال المفتاح، وإعادة تثبيت PWA.
- ⏳ تقسيم `store.ts`/`sync.ts` إلى وحدات — عند أول عمل كبير يمسّها.
- ⏳ ترقية Recharts 3 وReact/Next الرئيسية — كل واحدة على حدة باختبار بصري.

## ما رُفض من التقرير صراحةً (حتى لا يُعاد اقتراحه)

- CRDT / سجل تغييرات / mutation queue / device watermarks — بديلها المرحلة 1+2.
- إعادة تصميم بصرية شاملة — الهوية الحالية تُحمى، والتحسين توسيع لا هدم.
- مصفوفة اختبار الأجهزة الكاملة (قسم 17) كشرط «سلامة» — يكفي جهازا المالك
  الفعليان + اختبارات الدمج الآلية.
- اختبارات property-based للدمج — اختبارات الحالات الصريحة في المرحلة 1 كافية.
- WebAuthn/Passkey — وصف PIN الصادق الحالي («قفل شاشة لا تشفير») كافٍ.

## الإصدار النهائي المستقر — تتبّع البنود (P0–P8)

> عملٌ مرحليّ نحو إصدارٍ كبيرٍ مستقر يليه تجميد ميزات. يُنفَّذ في commits صغيرة
> واضحة، السلامة والمنطق قبل البصريات. البوابة بعد كل مرحلة:
> `npm test` · `npm run lint` · `cd cloudflare-worker && npm run typecheck` · `npm run build`.

### ✅ P0 — سلامة دمج حفظ القرآن (مُنجَز، مختبَر)
النموذج القديم (`plan ?? `, `Math.max(frontierId)`, اتحاد أعمى) كان يخلط الأجيال
ويُعيد خططاً مُسِحت ويُلغي تصحيح الجبهة للخلف. الحل: **جيل خطة** (`planId`) +
طابع موثوق (`planUpdatedAt`/`frontierUpdatedAt`) و`at` للجلسات.
- اختلاف الجيلين → الأحدث يفوز كاملاً (خطته وجبهته وسجلّاته وحده).
- اتّفاق الجيلين → اتحاد بلا فقد؛ الجبهة تحمي التقدّم المتزامن (max على الجلسات)
  ولا تُلغي تصحيحاً يدوياً حديثاً (frontierUpdatedAt الأحدث من آخر جلسة يفوز).
- ترحيل v13 بمعرّف جيل مشتقّ ثابت (`legacyHifzGen`) للبيانات القديمة بلا فقد.
- **migrations:** store v12→v13 (حقول اختيارية فقط؛ نُسخ احتياطية/مزامنة متوافقة —
  Firestore بـ`ignoreUndefinedProperties`). **اختبارات:** 12 سيناريو صريح في
  `merge.test.ts` (مسح/بدء جديد/تصحيح للخلف/جلستان متزامنتان/مراجعة+خطأ من
  جهازين/ترحيل قديم/استعادة نسخة لا تُعيد الممسوح). الملفات: `types.ts` ·
  `merge.ts` (`mergeHifz`,`legacyHifzGen`) · `store.ts` · `merge.test.ts`.

### ◐ P1 — مدرّب قرآني (أساس مُنجَز؛ الواجهة مؤجّلة)
- ✅ **أساس المراجعة المتباعدة** `quran/schedule.ts` (مُشتَق من السجلّ، بلا حالة
  جديدة في AppData): سلّم فترات صريح 1/3/7/14/30/60، `pageSchedules`/`duePages`/
  `dueQueue` (الأشدّ تأخّراً أوّلاً، حدٌّ يومي يوزّع الفائض)، و`todaySession`
  (سبق/قريبة/مستحقّة/أخطاء/وقت). 16 اختباراً (`schedule.test.ts`).
- ✅ **شاشة «جلسة اليوم» المتدرّجة + بطاقة الجلسة** (`TodaySession.tsx`): بطاقة
  علوية (حفظ جديد/مستحقّ/أخطاء/وقت + زر واحد) تختفي حين لا شيء لليوم، والأقسام
  المنفردة تبقى تحت فاصل. تدفّق: سبق ← قريبة ← مستحقّة ← جلسة أخطائي ← نتيجة،
  يعيد استخدام `HifzCoach`. هدف المراجعة اليومي جهازيّ (localStorage، بلا AppData).
- ✅ **إصلاح `weakSpots`**: الحالة مرتبطة بالوجه (تداخل) لا بمفتاح نطاق نصّي
  مطابق — وجهٌ ضعيف يُتقَن ضمن مدى مختلف يُصحَّح الآن (`latestRatingByPage`،
  دمج المتّصل). 6 اختبارات.
- ✅ **تطوير الأخطاء**: سياق الآية مع تظليل الكلمة، «آخر مرّة»، وعدّاد التكرار،
  واقتراح إغلاق تلقائي بعد تسميعٍ ناجحٍ متكرّر (`mistakeRecallSuccesses` مُشتَق،
  بلا حذف التاريخ) — و«جلسة أخطائي» ضمن تدفّق جلسة اليوم. 2 اختبار.

### ✅ P2 — دقّة الحفظ والسجل والتصحيح (مُنجَز)
- **الوتيرة الواقعية**: `hifzPace` يعرض وتيرة أيام النشاط + الواقعية (آخر 30
  يوماً شاملةً الخمول) + موعد إتمامٍ على الواقعية، ولا يُقدّر عند قلّة البيانات
  (`enough`). 
- **`spanPages`** الآن بالأوجه الفعلية من page metadata (لا نسبة عامة).
- **ربع/نصف الوجه**: `portionEnd` يتراكم آيةً آيةً بوزن صفحة كلٍّ (1/طولها) فيبلغ
  المقدار بدقّة عبر حدود الأوجه المتفاوتة (لا نسبة الوجه الأول مضروبةً).
- **سجل الحفظ والمراجعة** (`HifzLog`): تاريخ/مدى/نوع/تقييم، تعديل التقييم، وحذفٌ
  قابل للتراجع (Undo). حذف/تعديل جلسةٍ يعيد المتجرُ حساب الجبهة من الجلسات
  (`deleteHifzSession`/`restoreHifzSession` + `frontierUpdatedAt`) فيبقى الرسمُ
  والسجلّ متّسقَين.
- **بدء خطة جديدة**: تأكيدٌ يشرح ما سيُمسح (عدد الجلسات/المراجعات/الأخطاء
  والجبهة) + زر تصدير نسخة احتياطية (`backupFile.ts`، صيغة متوافقة مع المستورد).
- 8 اختبارات (`hifz.test.ts`): ربع/نصف عبر الأوجه، `spanPages`، والوتيرة الواقعية.

### ◐ P3 — المصحف والختمة (جارٍ)
- ✅ **تقدّم الختمة بالصفحة** (`khatma.ts` + `KhatmaOrbit`): زر «قرأت حتى الصفحة…»
  يشتقّ الأجزاء والنسبة، وتقديرُ موعد الإتمام على الوتيرة — **مع الحفاظ على الحلقة
  الثلاثينية**. `KhatmaState.page?` حقلٌ اختياري (بلا ترحيل). 5 اختبارات.
- ✅ **«تابع من حيث توقفت»**: علامةٌ محلّية بالجهاز لآخر سورةٍ فُتحت + زرُّ متابعة.
- ✅ **إعدادات قراءة خفيفة**: حجم النصّ، تباعد الأسطر، **وضع تركيز** يخفي الأدوات،
  و**Wake Lock** أثناء القراءة (بكشف الميزة فقط) — كلّها محلّية بالجهاز.
- ✅ بحث السور (قائم) وتنقّل السورة السابقة/التالية.
- ⏳ بحثٌ في نصّ الآيات المحلّي + قائمة سياق الآية (علامة/تأمّل/نسخ/تسميع) + تنقّل
  بالجزء/الوجه — إضافاتٌ لاحقة. الصوت مؤجّل حتى مصدرٍ موثّق (لا نربط الاستقرار به).

### ✅ P4 — الإدخال والتدبّر والإحصائيات (مُنجَز)
- **التدبّر**: تأمّل معبّأ من الآية (سابقاً)، **مسودّة تلقائية** (localStorage)،
  **وسوم** (إيمان/صبر/رزق/دعاء… + حرّة)، و**بحث وفلترة** بالوسوم — بلا تفسير آليّ.
  `tags?` حقلٌ اختياري على `QuranReflection` (يتّحد بالـid، بلا ترحيل).
- **إدخال القرآن في اللوحات**: `DayView` (حفظ/مراجعة/تأمّل/وِرد اليوم + شارة قرآن) ·
  `WeeklyWrap` (حصيلة قرآنية أسبوعية + إشارة في سماء الأسبوع) · `SmartInsights`
  (P7) · الإحصائيات (`HifzStatCard` قائمة).
- **تقرير أسبوعي قرآني** (`quranWeeklyReport` + `QuranWeekReport`): ما حُفظ/رُوجع،
  عدد الجلسات، أقدم مراجعة مستحقّة، أكثر موضع خطأ، وخطوة الأسبوع القادم. 3 اختبارات.

### ◐ P5 — سماء الذكريات والمذكرات (جارٍ)
> ✅ سماء تكيّفية (`memorySky.ts`+`MemorySky`): كوكباتٌ شهرية فوق 120، تركيزٌ متنقّل
> بدل مئات tabIndex=0، بطاقة معاينة، والفلاتر تنعكس — 6 اختبارات (334/1000). ✅
> «شعور اليوم» اختياريّ (`mood`، بلا ترحيل، يظهر بعد الكتابة). ⏳ محرّر fullscreen +
> «إضافات» + «سطر سريع» + «حصيلة الشهر» — تُترك لمعاينة المالك (تفضيلٌ ذوقيٌّ).
>
> [الوصف الأصلي:]
سماء تكيّفية (نجوم ثمّ كوكبات شهرية عند 80–120+، roving focus، معاينة عند اللمس،
اختبار 334 و1000) · محرّر مذكرة كامل الشاشة على الجوّال + إدخال تدريجي («إضافات»)
+ «سطر سريع» + «شعور اليوم» اختياري · «حصيلة الشهر».

### ◐ P6 — الأموال والإدخال (جارٍ)
- ✅ **`TransactionForm` معاد الترتيب**: المبلغ ← المتجر/الملاحظة ← اقتراح التصنيف
  («نقترح: كماليات ← قهوة» بقبول/تغيير)؛ شبكة التصنيفات متاحة لكن لا تُجبر قبل
  كتابة التاجر؛ التاريخ ومصدر الصرف تحت «تفاصيل» مطويّة؛ اختصارات آخر 5 متاجر و
  «كرّر آخر مصروف» (يملأ بلا حفظ تلقائي)؛ تعلّم قاعدة التاجر عند التغيير اليدوي.
- ✅ **توحيد اسم الصفحة** «الأموال» (كان العنوان «المصاريف» والوجهة «الأموال»).
- ✅ **المقارنة المالية** بنفس عدد الأيام (ضمن P7) وتفرّق عدم الصرف عن عدم التسجيل.
- ✅ إعادة تقسيم الصفحة الكامل (نظرة اليوم/الخطة/السجل) — **مُنجَز** ومعتمدٌ من
  المالك؛ التفاصيل في قسم «إعادة تنظيم صفحة الأموال في ثلاث طبقات» أدناه.
- ✅ **الأقساط** قسمٌ قابل للطيّ داخل الخطة المالية (تفاصيله في قسمه أدناه).

### ✅ P7 — بوصلة مدار (مُنجَز)
- **`Insight` ككائن منظّم** (`insights.ts`): id/domain/title/body/tone/priority/
  href?/actionLabel?/validUntil?/dedupeKey/reason?/dismissible/snoozeOptions — لا
  `text.includes` بعد الآن (رابط النسخة الاحتياطية صار `href`). إزالة تكرارٍ
  بالمفتاح وترتيبٌ بالأهمية.
- **بيانات القرآن في المحرّك**: ورد الحفظ، المراجعات المستحقّة (`todaySession`/
  `dueQueue`)، أقدم وجه متأخّر، الخطأ المتكرّر، الخطأ الجاهز للإغلاق، الاختبار
  الدوري، و«الخطوة التالية» (اكتمل القرآن → اقترح القراءة).
- **الواجهة** (`SmartInsights` → «بوصلة مدار»): توصية رئيسية «خطوتك الآن» +
  ثانويتان فقط، لكلٍّ زر إجراء واضح؛ تأجيل (اليوم/غداً/أسبوع) وإخفاء.
- **التفضيلات جهازية** (`insightPrefs.ts`، localStorage، لا تُزامَن) بدوالّ نقيّة
  مختبَرة (`filterInsights`/`snoozeUntilDate`).
- **المقارنة المالية** الآن بنفس عدد الأيام من الشهر الماضي (لا شهر جزئي بكامل).
- **`assistantContext.ts`**: أُزيل `buildAssistantContext` (نصّ حرّ من مساعدٍ
  قديم يضمّ مقتطفات مذكرات خام، غير مُستدعى) وبقي `buildDayDigest` المُستعمَل —
  فلا منطقان متكرّران، ولا استنتاج حالةٍ من نصّ المذكرة.
- 8 اختبارات (`insights.test.ts`): بنية الكائن، تفرّد المفاتيح، توصية القرآن،
  والتصفية بـvalidUntil/تأجيل/إخفاء.

### ◐ P8 — تحسينات عامة دون تغيير الهوية (جارٍ)
- ✅ **`PrayerOrbit` tick كل دقيقة**: نبضةٌ تُحدّث «الآن» فتنزلق نقطة الوقت وتُعاد
  الصلاة القادمة، وإعادة حساب المواقيت عند تغيّر اليوم أو الموقع — **دون أيّ مسٍّ
  للتصميم أو القوس أو الألوان**.
- ✅ **DayView** نشاط القرآن (P4).
- ✅ تعيينات ليلية لكلّ سطحٍ جديد (كلّ المكوّنات المُضافة تحمل `dark:`).
- ◐ أداء: `HifzLog` مُصفّح (pagination)، وسماء الذكريات تُجمّع عند الكثرة (P5).
- ✅ ضبط «اليوم المكتمل» في `dayAggregator` ليحترم الطقوس المجمّدة ويحتسب القرآن
  (وِرد/حفظ/مراجعة/تدبّر): الطقوس النشطة الثلاثة (مذكرة · قراءة · وِرد)، ويُستثنى
  المجمّد فلا يكسر الاكتمال. اختبارات في `dayAggregator.test.ts`.
  ✅ **ثمّ وُحِّد التعريف مركزياً** فلا نسخةَ ثانية في `utils.ts` ولا ثالثةَ في
  `stats/page.tsx` (راجع «تكملة المرحلة 2» أعلاه).

### ✅ جولة معالجة ملاحظات المراجعة
- **المصحف/الحفظ**: «تابع من حيث توقفت» يحفظ السورة *والآية* (والصفحة مشتقّة)؛
  تنقّلٌ بالسور/الأجزاء/الصفحة؛ بحثٌ في نصّ الآيات (تجريدٌ خاصّ بالرسم لا يمسّ
  الحروف)؛ قائمة إجراءاتٍ لكلّ آية (علامة · تأمّل · نسخ · تسميع)؛ هدف صفحاتٍ
  يوميّ للختمة؛ وتقدير الختمة على الوتيرة الأخيرة (14/30 يوماً) لا منذ البداية.
- **المذكرات**: محرّرٌ بملء الشاشة بدل النافذة؛ «سطر سريع»؛ قسم «إضافات» مطويّ؛
  «حصيلة الشهر».
- **الأموال**: «أيام بلا صرف» تُميّز عدم الصرف عن عدم التسجيل؛ إصلاح حساب الشهر
  السابق (لا `Date.setMonth` فائض في 29–31)؛ روابط عميقة لأقسام الصفحة.
- **سماء الذكريات**: إصلاح شرط الفراغ — كان يعتمد `stars.length` فتظهر «السماء
  الخالية» فوق الكوكبات عند تجاوز الحدّ (>120)؛ صار على `entries.length`. اختبار
  مكوّن فعليّ (`MemorySky.test.tsx`) عند 334 و1000 مذكرة يثبت ظهور الكوكبات.
- **دمج حفظ القرآن (P0)**: حذف الجلسات/المراجعات/الأخطاء صار يصمد أمام المزامنة
  عبر شواهد حذفٍ خاصّة بالجيل داخل `HifzState` (يرفعها التراجع)، فلا يعيدها اتّحادُ
  `mergeHifz` من جهازٍ قديم. و`updatedAt` رقميّ للسجلّ يُفوّز آخر تعديلِ تقييمٍ
  عبر جهازين، و`at` يرتّب سجلّي اليوم نفسه ترتيباً موثوقاً مستقلاً عن «الأساس».

### ✅ إعادة تنظيم صفحة الأموال في ثلاث طبقات (بصريّ فقط)
دون مسّ أيّ معادلةٍ ماليّة أو مزامنةٍ أو `AppData` أو حذفِ ميزة:
- **نظرة اليوم**: بطاقةٌ رئيسية للمتاح المتراكم (حمراء عند السالب) + شبكة 2×2
  (صرف الشهر · أيام إلى الراتب · أقرب التزام ومبلغه · مجموع الاحتياطيات)، كلٌّ
  ينقل إلى قسمه، ودعواتُ إعدادٍ صادقة عند نقص البيانات. المنطق في
  `financeOverview.ts` (نقيّ، يعيد استعمال دوالّ الحساب القائمة).
- **الخطة المالية**: أقسامٌ قابلة للطيّ (`CollapsibleSection` بـ`aria-expanded`/
  `aria-controls`، هدف لمس ≥44px) — الميزانية اليومية · سقوف التصنيفات
  (+تصنيفاتي) · المتكررة والقادم (+إدارة) · الأقساط · الاحتياطيات. رأسُ كلٍّ يحمل ملخّصاً
  صغيراً وشارةَ تحذيرٍ تبقى ظاهرةً وهو مطويّ. حالة الفتح محفوظةٌ جهازياً (لا تُزامَن).
- **السجل**: بحثٌ وفلترةُ شهرٍ وتقويمٌ وقائمةٌ أحدثَ أوّلاً مع «إظهار المزيد»،
  وزرُّ إضافةٍ قريبٌ لا يغطّي العناصر.
- الروابط العميقة `#daily/#budgets/#reserves` مستمرّة، وأُضيف `#recurring`
  و`#installments` و`#history`؛ الرابط يفتح القسم المطويّ قبل التمرير، و`?open=add|recurring|
  categories` مستمرّة. تحقّقٌ بصريّ: iPhone/iPad، فاتح/داكن، ببيانات وبدونها.

### ✅ الأقساط — قسمٌ داخل صفحة الأموال (الإصدار 0.1.292)

**المبدأ الحاكم**: الخطة **وصفٌ للاتفاق**، والمعاملات هي الحقيقة. لا شيء يُحتسب
مدفوعاً إلا بمعاملةٍ مربوطة، فمرورُ الموعد لا يدفع قسطاً، ولا يتضخّم صرفُ الشهر
بأرقامٍ لم تُنقَل من الحساب.

- **الموضع**: قسمٌ قابل للطيّ اسمه «الأقساط» داخل طبقة «الخطة المالية»، **بعد
  «المتكررة والقادم» وقبل «الاحتياطيات»**، مع رابطٍ عميق `‎/finance#installments‎`.
  لا صفحة جديدة ولا تغييرَ في التنقّل.
- **بيانات الخطة** (`InstallmentPlan` في `types.ts`): الجهة · اسم الالتزام ·
  السعر النقدي (اختياري) · السعر الإجمالي · الدفعة الأولى · قيمة القسط · عدد
  الأقساط · أول موعد استحقاق · الرسوم (توضيحية) · دفعة أخيرة كبيرة (اختياري) ·
  الحالة (نشطة/مسددة/ملغاة) · تصنيف · `createdAt`/`updatedAt` · ربطٌ اختياري
  بالتزامٍ متكرّر (`recurringId`).
- **الإجمالي هو المرجع الوحيد**: الرسوم لا تُضاف عليه، و`finalPayment` **تستبدل**
  آخر قسط ولا تُضاف صفّاً. عند عدم التطابق يظهر **تحذيرٌ غير معطِّل** يذكر ما تجمعه
  البنود والفرق (`planMismatch`) — لا نصحّح أرقام المالك ولا نمنع الحفظ.
- **جدول الاستحقاق** (`installmentDueDates`): توليدٌ شهريّ من تاريخ البداية بيوم
  مرساةٍ ثابت، يُقلَّم لآخر يوم الشهر (31 يناير → 28 فبراير، و29 في السنة الكبيسة)
  ثمّ **يعود ليوم المرساة** فلا يزحف الموعد للأبد. الجدول **مشتقٌّ لا مخزَّن**، فأيّ
  تعديلٍ على البنود يعيد حسابه فوراً بلا نسخةٍ متخلّفة.
- **تسجيل المدفوعات**: `Transaction` وُسِّع بـ`planId` · `planRole`
  (دفعة أولى / قسط / دفعة أخيرة / سداد مبكر) · `planInstallmentNo` (اختياري) ·
  `planLinkedAt`. الدور **حقلٌ مفرد** فلا تمثّل معاملةٌ أكثر من دورٍ واحد بنيوياً.
  الدفعة بلا رقمٍ تُنسَب لأقدم قسطٍ غير مكتمل؛ وعدّة دفعاتٍ تغطّي قسطاً واحداً.
- **الحسابات** (`planSummary`): المدفوع = مجموع المعاملات المربوطة · المتبقّي =
  `max(0, totalPrice − المدفوع)` · نسبة الإنجاز · القسط القادم · عدد الفائت.
- **السداد المبكر**: يُسجَّل **المبلغ الفعليّ وحده**، ولا يُخلَق مصروفٌ (ولا إيراد)
  وهميّ للفرق — الفرق يُعرَض «موفَّراً» (`saved`)، والأقساط الباقية تُوسَم
  «أُغلقت بالسداد المبكر» فلا هي مدفوعةٌ ولا متأخّرة.
- **الإلغاء والحذف**: الإلغاء يوقف المطالبة **ولا يحذف أيّ معاملة**، والحذف
  **شاهدٌ فقط** (tombstone) والمعاملات تبقى في السجل — ما دُفع مصروفٌ حقيقيّ.
- **الالتزامات المتكرّرة**: `generationMode: "auto" | "reminder"`، والقديم = auto.
  خطط الأقساط تستعمل `reminder` حصراً (لا تولّد معاملات)، ويحمل الوسم في
  `RecurringManager` فلا يتساءل المالك لماذا لم يُسجَّل تلقائياً. **منع التكرار في
  «أقرب التزام»**: قاعدة التذكير المرتبطة بخطةٍ نشطة تُستبعَد فيظهر الالتزام مرّةً
  واحدة (`nearestCommitment`).
- **واجهة اليوم**: لا بطاقة أقساطٍ دائمة؛ الأقساط تندمج في بطاقة «أقرب التزام»
  (تنقل لقسمها)، و**تنبيهٌ واحد فقط** عبر `SmartInsights`
  (`finance:installment-due`) لأقرب قسطٍ مستحقٍّ أو متأخّر.
#### الشراء المؤجّل («مهب كاش») وتبسيط التسجيل اليوميّ — 0.1.293

المشكلة التي عالجها هذا التعديل: شراءٌ بالتقسيط **لا يخرج من الحساب** لحظةَ
الشراء. لو سُجّل مصروفاً (يدوياً أو من رسالة بنك) ثمّ سُجّلت أقساطُه، حُسِب المبلغ
**مرّتين**: 1200 مرّةً، ثمّ 12×100.

- **«الأصل المؤجّل»** (`Transaction.deferred` + `planRole: "principal"`): التزامٌ لا
  صرف. يظهر في السجل بوسم «مؤجّل — لا يُحتسب» (بلا إشارةٍ سالبة ولا لونٍ أحمر)
  ويُحتسب **صفراً في كل حساب**: الميزانية اليومية · السقوف · صرف الشهر · بطاقة
  اليوم · الرسوم البيانية · الإحصائيات · بوصلة مدار. الأقساط وحدها هي الصرف.
- **بوابةٌ واحدة** لهذا القرار: `cashOut(t)` / `isCashOut(t)` في `utils.ts`. كل
  تجميعٍ للصرف في التطبيق (١٦ موضعاً) يمرّ منها بدل `t.amount` المباشر، و
  `deferredSpend.test.ts` يمسك الضمانة عبر الوحدات كلّها — فأيّ تجميعٍ جديد يقرأ
  المبلغ خاماً يُكشَف في الاختبار لا في جيب المالك.
- **«قسّط هذا المصروف»** (`convertTransactionToPlan`): من نموذج تعديل أيّ مصروف —
  ثلاث خانات فقط (الجهة · عدد الأقساط · أول موعد)، والقسط **يُشتقّ بالقسمة** وقابل
  للتعديل. الإجمالي = مبلغ المعاملة نفسه (لا يُكتب مرّتين)، والمعاملة تصير الأصل
  المؤجّل. `unlinkTransactionFromPlan` يرجعها مصروفاً عادياً.
- **الطريق اليوميّ للدفعات** (هدفه: أقلّ ضغطةٍ ممكنة):
  1. **«سجّلت الدفع»** في رأس قسم الأقساط لأقرب قسطٍ مستحقٍّ عبر كل الخطط، وعلى كل
     بطاقة خطة — ضغطةٌ واحدة تسجّل القسط بمبلغه وتاريخ اليوم بلا فتح جدولٍ ولا
     اختيار رقمٍ أو تصنيف (`payNextInstallment`).
  2. **ربطُ مصروفٍ سجّلتَه أصلاً**: نموذج المصروف يعرض شريط «دفعة قسط؟» بالخطط
     المفتوحة — **الخطة التي يطابق قسطُها المبلغ المكتوب تتقدّم** (حالة رسالة
     البنك)، والضغطة تربط بلا إدخالٍ مزدوج (`linkTransactionToPlan`).
- **نموذج الخطة أخفّ**: **اشتقاقٌ تلقائيّ** — اثنان من (الإجمالي · القسط · العدد)
  يكفيان والثالث يُحسب ويظهر في الخانة الفارغة، فالتحذير لا يظهر في الحالة
  الشائعة. والنقديّ/الرسوم/الدفعة الأخيرة/التصنيف/الملاحظة تحت «تفاصيل إضافية»
  مطويّة → الظاهر ستّ خانات.
- **الأصل ليس دفعة**: `planPayments` تستثني دور `principal` وأيّ معاملةٍ مؤجّلة،
  وإلا ظهرت الخطة مسدّدةً كاملةً في لحظة الشراء. `planSummary.principal` للعرض،
  وتحذيرٌ غير معطِّل إن خالف مبلغ الأصل إجماليَّ الخطة.

#### معالجة ملاحظات المراجعة المستقلّة — 0.1.294

خمس نقاط من مراجعةٍ مستقلّة على `5baf888`، كلّها أُصلحت بلا ميزةٍ جديدة ولا إعادة
تصميم:

1. **تنبيه القسط**: النافذة صارت **أسبوعاً** (كانت 3 أيام — قد تفوت آخر فرصةٍ
   للتحويل)، و`dedupeKey` صار خاصّاً بالقسط بعينه:
   `finance:installment-due:<planId>:<due>`. كان مفتاحاً واحداً لكل الأقساط، فإخفاء
   تنبيه قسطِ هذا الشهر يُسكِت الخطة **إلى الأبد**. اختباراتٌ للإخفاء والتأجيل
   وانتقال التنبيه إلى القسط التالي بمفتاحٍ جديد.
2. **تاريخ الدفع الفعليّ**: زرّ «سجّل» للقسط المتأخّر كان يكتب المعاملة بتاريخ
   **الاستحقاق القديم** — فيغيّر صرف شهرٍ مضى وميزانيته المرحّلة. الآن `today()`
   دائماً، وموعد الاستحقاق يبقى في جدول الخطة وحده.
3. **القسط المدفوع جزئياً**: `rowRemaining(row)` — «سجّلت الدفع» يسجّل **الباقي
   عليه وحده** (60 من 100 لا 100 ثانيةً)، والعرض يذكر «الباقي عليه» وما دُفع منه.
4. **«٣ من ١٢ قسطاً»** صريحاً على بطاقة الخطة (`paidRows`/`totalRows` في
   `planSummary`) — أوضح من النسبة وحدها.
5. **المؤجّل في `DayView`**: رمادياً بوسم «مؤجّل — لا يُحتسب» وبلا إشارةٍ سالبة،
   وإجمالي اليوم يبقى على `cashOut`.
6. **«أكبر مصروف»** في متابعة الصرف: `biggestCashExpense` (نقيّ ومختبَر) يقارن
   بـ`cashOut` — كان أصلٌ مؤجّل بـ1200 يتصدّر القائمة وهو لم يُدفع. اختبارٌ للحالة
   بالحرف (1200 مؤجّل + 100 قسط → القسط هو الأكبر).
7. **الحرّاس في الطريق الثاني**: `MAX_INSTALLMENT_COUNT` (120) و**تاريخٌ صالح فعلاً**
   (`isValidDateKey` — «2026-02-30» و«2026-13-01» تُرفض، لا الشكل وحده) مطبَّقان في
   `SplitToPlanForm` و`convertTransactionToPlan` كما في النموذج الرئيسي، فلا يقبل
   طريقٌ ما يرفضه الآخر. والمعاملة لا تُلمَس عند الرفض (لا تصير مؤجّلةً بالغلط).

- **التخزين والمزامنة**: `installmentPlans` في `AppData` · persist **v14** بمهاجرةٍ
  كاملة · دمجٌ بالـ`id` و`updatedAt` (`byIdNewer`) · شواهد حذف عبر `ID_COLLECTIONS`
  · `hasData`/`cloudHasUnseen` يعرفانها · النسخ الاحتياطية تحملها
  (`snapshot`/`normalizeBackup`).

#### الأقساط من جديد + الأصول والإهلاك — 0.1.295

**الشكوى بالحرف**: «تو شريت شي بالأقساط، أول دفعة ١٥٠٠ ريال ثم أقساط متساوية ٧٨٠
ريال كل شهر لمدة ٦ أشهر — بالطريقة الحالية ما قدرت أسجّلها أبداً.» النموذج القديم
كان يفتح بسؤال **السعر الإجمالي**، وهو الرقم الوحيد الذي لا يعرفه المشتري. أُعيد
بناء المدخل كلّه حول قصّة الشراء نفسها، مع إبقاء المنطق النقيّ (`installments.ts`)
ومبدأه الحاكم: **الخطة وصفُ اتفاقٍ لا مصروف**.

- **النموذج على ثلاث خطواتٍ مرقّمة**: ١ وش اشتريت · ٢ كم دفعتَ أوّلاً (بمبلغه
  وتاريخه) · ٣ القسط الشهري وعدد الشهور. **لا خانة للإجمالي أبداً** —
  `partsTotal` يحسبه ويعرضه حيّاً: «الإجمالي ٦٬١٨٠ = ١٬٥٠٠ + ٧٨٠ × ٦ · آخر قسط
  …». فلا تحذيرَ تطابقٍ أصلاً (لم يعد له معنى: لا رقمين ليختلفا).
- **تلقائيٌّ بلا طلب** (`createInstallmentPlan`، خطوةٌ واحدة في المتجر): الدفعة
  الأولى تُسجَّل **مصروفاً حقيقياً بتاريخها** لأنها خرجت فعلاً (وعلامةٌ لإيقافها
  إن كان سجّلها بنفسه)، والتذكير الشهري (`reminder`) يُربط تلقائياً، وأوّل موعد
  قسطٍ يُقترح بعد شهرٍ من الدفعة الأولى. **ولا معاملة لقسطٍ قادم أبداً.**
- **الربط التلقائي للدفعة** (`suggestPlanLink`، نقيّ ومختبَر): مصروفٌ بمبلغ قسطٍ
  مستحقٍّ (±١ ريال) قرب موعده (±١٢ يوماً) → تُختار خطته من نفسها في نموذج المصروف
  («رُبطت تلقائياً»)، ويبقى الإلغاء بضغطة. **الشكّ يمنع الربط**: مرشّحان = لا ربط.

**الأصول والإهلاك** (`src/lib/assets.ts` · قسم «الأصول» في الخطة المالية):

- الأشياء الغالية التي تملكها وتخدمك سنين، بإهلاكٍ **خطّيٍّ يوميّ** «على كيفك»:
  اليوميّ = (الثمن − القيمة المتبقّية) ÷ العمر بالأيام. أعمارٌ جاهزة (سنة… ١٠
  سنوات) وحقلٌ حرٌّ بالشهور.
- تجيب: **كم يكلّفني في اليوم**، كم استُهلك منه، وكم بقي من قيمته ومن عمره —
  ومجموعٌ لكل الأصول (يومياً · هذا الشهر · القيمة الدفترية). البيع يوقف العدّاد
  ويقارن الثمن بالقيمة الدفترية (ربح/خسارة).
- **لا يمسّ الصرف بحال**: لا معاملة، ولا ميزانية يومية، ولا سقوف، ولا إحصائيات —
  المصروف الحقيقي هو الشراء (أو أقساطه) حين سُجّل، ولا يُحتسب مرّتين. وربطٌ اختياريّ
  بخطة الأقساط (علامةٌ في نموذج الخطة تُنشئ الأصل بإجمالي التقسيط).
- **التخزين والمزامنة**: `assets` في `AppData` · persist **v15** بمهاجرةٍ فارغة ·
  دمجٌ بالـ`id` و`updatedAt` (`byIdNewer`) · شواهد حذف عبر `ID_COLLECTIONS` ·
  النسخ الاحتياطية تحملها.

#### الزرّ الصامت في نموذجَي الأقساط والأصول — 0.1.296

بلاغ المالك: «ولا زر يشتغل» في نموذج الأصول، ثمّ إنّ نموذج الأقساط مثله، وباقي
الصفحة حيّة والإصدار أحدث نسخة. المتصفّح هنا (Chromium، إنتاجاً وتطويراً، بلمسٍ
وبحجم جوّال) يحفظ بلا مشكلة — فالسبب المرشّح شرطٌ يمنع الحفظ **بلا أن يقول شيئاً**:

1. **تاريخٌ يُمحى من تحت الحقل**: الحفظ في النموذجين مشروطٌ بتاريخٍ صالح (تاريخ
   الشراء · أوّل موعد قسط)، و`<input type="date">` في Safari قد يُرجع قيمةً فارغة
   (يحدث حين يكون تقويم الجهاز هجرياً) فيصير الحقل بلا تاريخٍ صالح ويرفض الزرّ
   الحفظ. الآن: القيمة الفارغة **لا تمحو** التاريخ — نتمسّك بآخر تاريخٍ صالح.
   وباقي الصفحة لا تعتمد على حقول تاريخ، فبقيت حيّةً وبدا العطل خاصّاً بالنموذجين.
2. **رفضٌ صامت**: النواقص كانت تظهر بعد الضغط فقط. الآن تُحسب **حيّةً** أثناء
   الكتابة: الزرّ معطَّلٌ ظاهراً وتحته سببه («لإتمام الحفظ: ثمن الشراء مطلوب»)،
   فلا يُظنّ زرٌّ ميتاً وهو ممتنع.
3. **خللٌ لا يُبتلع**: `handleSave` في النموذجين داخل `try/catch` يعرض نصّ الخطأ
   في مكان التنبيهات بدل أن يختفي في وحدة التحكّم فيبدو الزرّ ميتاً.

#### القسم المكرَّر، والفعل المكرَّر، و«يبدو أنه قسط» — 0.1.297

بلاغ المالك بصورتين (آيباد وجوّال) لصفحة الأموال: القسم يظهر **مرّتين**، والعملية
**تتكرّر**، ثمّ طلبٌ صريح: «إذا كتبت القسط في التسجيل السريع، يعرف أنه قسط لا
مصروف عادي — لأني كاتبٌ التاريخ والدفعة».

1. **رأسٌ مكرَّر**: `InstallmentPlans` و`Assets` كانا يرسمان عنوانهما وأيقونتهما
   وشارتهما داخل `CollapsibleSection` الذي يحمل **نفسها** — فيقرأ العينُ «الأقساط
   · متبقٍّ 5,505» سطرين متتاليين. حُذف الرأس الداخليّ وبقي زرّ الإضافة وحده؛
   رأس القسم هو المصدر الوحيد للعنوان والملخّص.
2. **فعلٌ مكرَّر**: شريط «القسط القادم» فوق القائمة اختصارٌ **عبر الخطط**، لكن مع
   خطةٍ واحدة مفتوحة يصير هو نفسه زرَّ بطاقتها (نفس القسط، نفس المبلغ، نفس الفعل)
   فيبدو أنّ التطبيق كرّر العملية. صار لا يظهر إلا عند خطّتين مفتوحتين فأكثر —
   حيث يوفّر بحثاً فعلاً. ونصّ الزرّ صار أمراً («سجّل دفع القسط ١») بدل ماضٍ
   («سجّلت…») الذي كان يُقرأ إقراراً بدفعٍ لم يقع.
3. **«يبدو أنه قسط»**: `suggestPlanLink` يشترط نافذة ١٢ يوماً حول الموعد، فدفعةٌ
   تُسجَّل قبل موعدها بشهر تمرّ مصروفاً عادياً ويتخلّف جدول الخطة. أُضيفت
   `suggestPlanByAmount` (نقيّة ومختبَرة): مطابقةُ مبلغ **بلا نافذة تاريخ**،
   بشرط ألّا يحتمل غير **خطةٍ واحدة**. وهي **لا تربط تلقائياً أبداً** — مطابقة
   رقمٍ ليست يقيناً — بل تعرض سؤالاً في التسجيل السريع («يبدو أنه القسط ١ من
   ماك بوك M5 · مستحقٌّ خلال ٤٥ يوماً») يربطه المالك بضغطة. الربط التلقائي
   الصارم داخل النافذة باقٍ كما هو بلا تغيير.

#### الأصول لا تعبر بين الأجهزة — ثغرةٌ في قرار المزامنة — 0.1.298

بلاغ المالك: «الاختلاف بين الآيباد والآيفون إلى الآن موجود» — أصلٌ سُجّل على
الجوّال لا يظهر على الآيباد.

المزامنة ثلاثُ شبكات أمان متتالية: ختمٌ أحدث · ارتفاع `revision` · **محتوى لم
يُرَ** (`cloudHasUnseen`). حين تُضاف مجموعةٌ لـ`AppData` ولا تُضاف للثالثة، تسقط
الشبكة الأخيرة بصمت. `assets` أُضيفت في 0.1.295 وأُغفلت في `cloudHasUnseen`
**و**`hasData` معاً — فأصلٌ جديد في السحابة لا يُعدّ محتوىً جديداً على الجهاز
الآخر، ويبقى الجهازان مختلفَين بلا رسالة خطأ ولا مؤشّر.

أُضيفت `assets` للدالتين، مع تعليقٍ في `syncDecision.ts` يشترط إضافة كل مجموعةٍ
جديدة في الاثنتين، وحارسين في `syncDecision.test.ts` يكشفان الإغفال التالي.
الدمج نفسه (`mergeAppData`) كان سليماً — الأصول تتّحد بـ`byIdNewer` وحذفُها
مشهودٌ عبر `ID_COLLECTIONS` — فالخلل كان في **قرار** التبنّي لا في الدمج.

#### مدى حساب السقوف: مفتاحٌ في الإعدادات (دورة الراتب ↔ الشهر الميلادي) — 0.1.301

المالك تردّد بين النافذتين، فصار الخيار له بدل أن يُفرض: `budgetWindow`
(إعدادٌ مفرد مختوم بـ`fieldUpdatedAt`، افتراضه «دورة الراتب») يُبدَّل من بطاقة
«مدى حساب سقوف التصنيفات» في الإعدادات. `spendWindow()` في `budgetCycle.ts` هي
البوابة الوحيدة التي تترجم الخيار إلى نافذة (`YYYY-MM` أو تاريخ بداية الدورة)،
فلا يتكرّر الشرط في مكوّن. التبديل عرضٌ محض: لا يمسّ السقوف ولا المعاملات،
والميزانية اليومية المتراكمة لها دورتها الخاصة ولا يمسّها الخيار.

#### دورة الراتب تصفّر السقوف · الفائض المتوقّع · الأيام الصامتة في السماء — 0.1.300

بلاغ المالك: «إذا حطيت نزل الراتب تتصفّر الميزانيات ونبدأ ميزانيات جديدة… وأبغى
أعدّلها بعدين» · «أبغى أشوف الأيام اللي ما كتبت فيها، واللي فيها صور فقط» ·
«الفائض المتوقّع ما أحس أنه محسوب زين».

- **نافذة السقوف صارت دورة الراتب لا الشهر الميلادي**: `src/lib/budgetCycle.ts`
  (نقيّ ومختبَر) هو المصدر الوحيد — `budgetCycleStart` = تأكيد «نزل الراتب»
  (`lastSalaryConfirm`)، وإلا آخر يوم راتبٍ مرّ. الدورة تنتهي **بالضغطة** لا
  بمرور التاريخ، مطابقةً لِما يراه المالك في البانر. تمرّ منها كل جهةٍ تحسب صرف
  السقوف: `BudgetTracker` · `budgetAlerts` · `budgetWarningFor` · `FinancePace`.
- **السقوف نفسها لا تُمسّ** عند التصفير — تعريفها باقٍ، وصار **قابلاً للتعديل في
  مكانه** (قلم على كل سقف: مبلغ ثابت ↔ نسبة من الدخل) بدل حذفٍ وإعادة إضافة.
- **الفائض المتوقّع عند نزول الراتب** (`projectedCycleSurplus`): الرصيد المتراكم
  + يوميّة ما بقي من أيام − الصرف المتوقّع على **وتيرتك الفعلية في هذه الدورة**،
  ومعه السقف الأعلى (لو لم تصرف شيئاً) حتى لا يُقرأ الرقم على أنه وعد.
- **السماء**: `entryVoice` يميّز المذكرة بلا كلمات (صورة/صوت فقط) فتُرسم **حلقةً
  مضيئة** بدل قرصٍ مصمت، و`silentDates` يستخرج الأيام التي لم تُكتب في المدى
  المعروض فتظهر — بمفتاح «🌑 الأيام الصامتة» — كحلقاتٍ خافتة، ولمس أيّ واحدةٍ
  يفتح ذلك اليوم للكتابة فيه.

#### الشريط الجانبي على الآيباد: الإعدادات والمزامنة تظهران — 0.1.299

بلاغ المالك: «زر الإعدادات والمزامنة ما يظهر بالآيباد… بالزاوية تحت».

الشريط الجانبي `fixed` بارتفاع `min-h-screen`. و`fixed` لا يتمرّر معه شيء، و`100vh`
على Safari (آيباد/آيفون) **أطولُ من المساحة المرئية فعلاً** لأن شريط المتصفّح
ومؤشّر الهوم يغطّيان الأسفل — فصفُّ المزامنة والإعدادات والسمة، وهو آخر عنصرٍ في
العمود، يسقط تحت الحافة: لا يُرى ولا يُلمس ولا سبيل للتمرير إليه.

- `h-[100dvh]` بدل `min-h-screen`: الارتفاع يتبع المرئيّ حيّاً.
- التنقّل `flex-1 min-h-0 overflow-y-auto`: القائمة هي التي تتقلّص عند ضيق
  الارتفاع، لا الأطراف — فلا يُقتطع طرفٌ أبداً.
- **والأهمّ**: نُقل الصفّ من ذيل الشريط إلى **أعلاه** تحت الشعار مباشرةً — مكانه
  نفسه في ترويسة الجوّال. كان في أبعد نقطةٍ عن العين وأوّلِ ما يُقتطع، فبدا
  التطبيق على الآيباد بلا إعداداتٍ ولا حالة مزامنة بينما هي ظاهرةٌ على الآيفون.
- حشوُ منطقة الأمان في ذيل العمود فلا تجلس القائمة تحت مؤشّر الهوم.

وتأكيداً على الأقساط (سؤال المالك): تغطيتها كاملة — كلّ تعديلٍ على الخطة يختم
`updatedAt` (إضافة · تعديل · إلغاء · إعادة فتح · سداد · ربط تذكير)، والحذف مشهودٌ
عبر `ID_COLLECTIONS`، والدمج `byIdNewer`، وهي مفحوصةٌ في `hasData` و
`cloudHasUnseen`، ولها اختبارات دمجٍ قائمة. الأصول كانت بلا اختبار دمجٍ واحد رغم
أنها هي التي تخلّفت — فأُضيفت لها الاختبارات الثلاثة نفسها.

### ✅ نظام إصدار التطبيق — رقمٌ يعكس تاريخ التطوير (0.1.292)

- **القاعدة**: `APP_BUILD` في `src/lib/version.ts` عدّادٌ **تسلسليّ** لعدد
  التعديلات الجوهرية (ميزة · إصلاح · مهاجرة) منذ أوّل نسخة مستقرة. يرتفع بواحدٍ مع
  كل تعديل، **بلا قفزٍ وبلا إعادة تعيين**، و`APP_VERSION = 0.1.<build>`.
- **مصدرٌ واحد**: `version.ts` هو المصدر؛ `package.json` يطابقه، وصفحة الإعدادات
  تقرأ منه (كان الرقم مكتوباً بيدٍ في موضعين فتخلّف أحدهما بلا إشعار).
- **بوابة**: `scripts/check-version.mjs` يُشغَّل **قبل** `next build` ويكسر البناء
  عند أيّ انحراف. `npm run bump` يرفع الرقم في الملفين معاً.
- **نقطة الانطلاق**: 292 — تاريخ المستودع الحقيقي 291 تعديلاً وهذا التعديل هو
  292، فالرقم يعكس عمر المشروع لا إصداراتٍ رسمية. كلّ رفعٍ يُسجَّل سطراً هنا.
  - `0.1.292` — الأقساط + سلامة المزامنة (revision/المتكررة/اليوم المكتمل) + نظام
    الإصدار نفسه، persist v14.
  - `0.1.293` — الشراء المؤجّل («مهب كاش»): الأصل التزامٌ لا صرف عبر بوابة
    `cashOut` الواحدة، و«قسّط هذا المصروف»، وتسجيل الدفعة بضغطةٍ واحدة، ونموذجٌ
    أخفّ باشتقاقٍ تلقائيّ. بلا مهاجرة (حقلٌ اختياريّ فقط).
  - `0.1.294` — معالجة ملاحظات المراجعة المستقلّة: مفتاح تنبيهٍ لكل قسط + نافذة
    أسبوع · تاريخ الدفع الفعليّ · الباقي على القسط الجزئيّ · «٣ من ١٢» ·
    المؤجّل في DayView · «أكبر مصروف» بالنقد · حرّاس العدد والتاريخ في كل الطرق.
  - `0.1.295` — الأقساط أُعيد بناء مدخلها حول قصّة الشراء (بلا خانة إجمالي، دفعةٌ
    أولى تُسجَّل تلقائياً، ربطٌ تلقائيّ للدفعة بقسطها) + «الأصول» وإهلاكها اليوميّ،
    persist v15.
  - `0.1.296` — الزرّ الصامت في نموذجَي الأقساط والأصول: تاريخٌ فارغ من منتقي
    Safari لا يمحو الحقل · سبب تعطّل الزرّ ظاهرٌ حيّاً · خطأ الحفظ يُعرَض نصّاً.
  - `0.1.297` — صفحة الأموال: رأسُ «الأقساط»/«الأصول» لا يُرسم مرّتين · شريط
    القسط القادم لا يكرّر زرّ البطاقة عند خطةٍ واحدة · `suggestPlanByAmount`
    تتعرّف على القسط بالمبلغ خارج نافذة الموعد وتسأل قبل الربط.
  - `0.1.298` — الأصول لم تكن في `cloudHasUnseen`/`hasData`، فأصلٌ سُجّل على جهاز
    لا يُعدّ محتوىً جديداً على الآخر وتبقى الأجهزة مختلفة. أُضيفت مع حارسَي اختبار.
  - `0.1.299` — الشريط الجانبي `100dvh` بدل `min-h-screen`، وصفُّ المزامنة
    والإعدادات والسمة انتقل لأعلى الشريط (مكانه في ترويسة الجوّال) بعد أن كان
    يسقط تحت حافة الآيباد · اختبارات دمجٍ للأصول.
  - `0.1.300` — السقوف تُحسب على دورة الراتب (`budgetCycle.ts`) فتتصفّر مع تأكيد
    «نزل الراتب» وتبقى قابلة للتعديل · الفائض المتوقّع عند الراتب على الوتيرة
    الفعلية · السماء تميّز المذكرات بلا كلمات وتعرض «الأيام الصامتة».
  - `0.1.301` — `budgetWindow`: مفتاحٌ في الإعدادات يبدّل مدى حساب السقوف بين
    دورة الراتب (الافتراضي) والشهر الميلادي، عبر `spendWindow()` وحدها.
  - `0.1.302` — جولة سلامةٍ قبل التجميد: `hydrate` يطبّق `assets` (كانت غائبةً
    عن الدورة وحدها) مع حارس round-trip لكلّ حقول `AppData` · **طابعُ تعديلٍ
    لكلّ عنصر** يختمه غلاف `set` تلقائياً، والدمج يحسم به تعارض الكتب وجلسات
    القراءة والتأمّلات والصناديق والتصنيفات والعادات والرسائل والسقوف وقواعد
    التجار وحالة الصلاة والختمة وترتيب التصنيفات (كان تقدّم القراءة يرجع للخلف
    حين يفوز ختمُ مستند الجهاز الآخر) · النسخة الاحتياطية لم تعد تُسقط
    `frozenHabits` · جهازٌ جديد يتبنّى السحابة يمرّ بـ`applyTombstones` فلا تعود
    مذكرةٌ محذوفة من shard قديم · وصول: بطاقة «وِرد اليوم» زرٌّ ورابطٌ شقيقان
    (44px) بدل رابطٍ داخل زرّ، شعار الشريط الجانبي لم يعد `h1` ثانياً في الصفحة،
    وأسماء وصولٍ وأهدافُ لمسٍ لأزرار السقف وشهرَي تقويم الصرف.
  - `0.1.303` — تعارضُ العناصر **المركّبة**: لكلّ جزءٍ دمجُه. سجلّاتُ العادة
    وإيداعاتُ الصندوق ووسائطُ المذكرة لم تعد ترفع طابع العنصر (لها اتحادُها
    وشواهدُها)، فتسجيلُ يومٍ أو استكمالُ صور Day One لا يبتلع إعادةَ تسميةٍ أو
    تحريرَ نصّ على الجهاز الآخر · ختمُ الصلاة صار لكلّ `date + prayer` لا لليوم
    كاملاً (وإلغاءُ تسجيلٍ يسري) · `dailyPageGoal` بطابعٍ مستقلٍّ عن تقدّم الختمة
    مع بقاء `completed` لا ينقص · حارسُ الدورة صار `Required<AppData>` (و
    `normalizeBackup` كذلك) فيكشف الحقول **الاختيارية** الجديدة وقتَ الترجمة.

**البوابة الحالية:** `npm test` **414 أخضر** (كان 375) · `npm run lint` بلا أخطاء
مع 4 تحذيرات سابقة فقط · worker `typecheck` أخضر · `npm run build` ناجح ·
`npm audit --omit=dev` يفشل بيئياً (نقطة النهاية القديمة متقاعدة/محجوبة عبر
الوسيط) — الفحص البديل عبر نقطة `advisories/bulk` أظهر تنبيهين **سابقين** في
اعتماديّاتٍ غير مباشرة لـ`next` وقتَ البناء فقط (`postcss@8.4.31`،
`sharp@0.34.5`)، ولا يشحن منهما شيءٌ للمتصفّح في موقعٍ ثابت (`output: "export"`
مع `images.unoptimized`)؛ علاجهما ترقية `next` في جلسةٍ مركّزة.
مُنجَز: P0 سلامة الحفظ (الأجيال + شواهد حذف السجلّات) · P1 (طابور المراجعة،
«جلسة اليوم»، `weakSpots`، الأخطاء) · P7 «بوصلة مدار» · جولة ملاحظات المراجعة
(المصحف/المذكرات/الأموال/اليوم المكتمل/سماء الذكريات) · إعادة تنظيم صفحة الأموال ·
الأقساط · سلامة المزامنة (المستمع الحيّ/المتكررة/التعريف الموحّد) · نظام الإصدار.
المتبقّي: اختبار يدويّ على جهازين (يقوم به المالك).
